# chess-core docs

Local development: first run `bun install` in this `docs/` folder. Then, from the repo root run `bun run docs:dev` (or `bun run docs:build` to build), or from this folder run `bun dev` / `bun build`.

Built with [Starlight](https://starlight.astro.build). Content lives in `src/content/docs/`.
