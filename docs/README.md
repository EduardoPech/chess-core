# chess-core docs

Local development: from the repo root run `bun run docs:dev`, or from this folder run `bun dev`. Build with `bun run docs:build` or `bun build`.

Built with [Starlight](https://starlight.astro.build). Content lives in `src/content/docs/`.
