---
title: chess-core
description: Fast, type-safe chess library with immutable state and bitboards.
hero:
  tagline: A fast, type-safe chess library with immutable state and bitboard representation.
  actions:
    - text: Getting started
      link: /guides/getting-started/
      icon: right-arrow
    - text: API reference
      link: /reference/api/
      icon: open-book
      variant: minimal
---

## Quick start

```bash
npm install @pech/chess-core
# or
bun add @pech/chess-core
```

```typescript
import { fromFen, fromSan, makeMove, toSan, isCheckmate, STARTING_FEN } from '@pech/chess-core';

const pos = fromFen(STARTING_FEN);
const move = fromSan(pos, 'e4');
const newPos = makeMove(pos, move);

console.log(toSan(pos, move));      // "e4"
console.log(isCheckmate(newPos));   // false
```
